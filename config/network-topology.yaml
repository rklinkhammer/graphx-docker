# Generated from ../graphx.yaml by `graphx project`.
# Non-authoritative view: edit graphx.yaml, then regenerate.
networks:
  - id: graphx
    profile: ethernet
    subnets:
      - 172.30.0.0/24
    gateway: 172.30.0.1
    external: false
edge_paths:
  - edge: samples
    hops:
      - generator
      - graphx
      - transform
  - edge: transformed
    hops:
      - transform
      - graphx
      - sink
