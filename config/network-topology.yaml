# Generated from ../graphx.yaml by `graphx project`.
# Non-authoritative view: edit graphx.yaml, then regenerate.
networks:
  - id: graphx
    driver: bridge
    subnets:
      - 172.30.0.0/24
    gateway: 172.30.0.1
    external: false
interfaces:
  - id: data
    owner: generator
    network: graphx
  - id: data
    owner: transform
    network: graphx
  - id: data
    owner: sink
    network: graphx
edge_paths:
  - edge: samples
    hops:
      - generator
      - graphx
      - transform
  - edge: transformed
    hops:
      - transform
      - graphx
      - sink
