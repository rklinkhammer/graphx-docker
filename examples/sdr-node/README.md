# SDR node examples

The simulated profile runs without hardware. The external profile models a raw
device data plane, origin-owned control, TLS, OVS attachment, capture, history, and
telemetry. GraphX does not configure physical radio hardware; the operator owns
device setup and safety.
