# CMake example targets

The [top-level README](../README.md#build-all-examples) is the single build reference.
It covers explicit all-example artifact builds and the direct `graphx example`
laboratory workflow. CMake retains only `examples-list`, `examples-build`, and
`examples-rebuild` as convenience targets. Example-specific behavior remains in each
example's README.
