# CMake example targets

The [top-level README](../README.md#build-all-examples) is the single build reference.
It covers all-example builds, preparation, runtime targets, CMake cache settings,
Node, containers and Lima. Example-specific behavior remains in each example's README.
